# Minimal requirements for PALAScribe (server + transcription)
# Note: pin versions or run `pip freeze > requirements.txt` from your venv
# For Whisper (OpenAI) and PyTorch; install torch separately with the correct CUDA
# For CPU-only: follow PyTorch install instructions at https://pytorch.org/get-started/locally/

# Core ML / transcription
openai-whisper
torch

# Audio / signal processing
numpy
soundfile
librosa
ffmpeg-python

# Utilities
tqdm

# PDF generation (optional)
reportlab

# Optional: used by some helper scripts
python-dotenv

# If you prefer exact pins, run on your dev machine:
# pip freeze > requirements.txt
